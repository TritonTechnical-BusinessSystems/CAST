packages:
  - 'packages/*'
  - 'components/*'
allowBuilds:
  # better-sqlite3 is a native module — needs a C++ toolchain (build-essential)
  # to compile locally; it also builds in the Docker image (build deps included).
  better-sqlite3: true
  esbuild: true
